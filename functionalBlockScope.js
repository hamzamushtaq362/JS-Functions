{
    // when a function is declare within a code block, it’s visible everywhere inside that block. But not outside of it. they are visible eveerywhere in that block

    // Variables that are declared inside a function have functional scope. Variables can be accessed anywhere in the function.

    // Variables that are declared inside a block have block scope. Variables can only be accessed in the block.
}